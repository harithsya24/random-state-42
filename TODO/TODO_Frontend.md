# TODO: Frontend / UX

1. Real-time push
   - Replace polling with WebSocket or SSE to lower latency and bandwidth.

2. Map performance
   - Add marker clustering, limit donor markers, lazy-load layers, optimize polylines.

3. UX improvements
   - Add detailed transfer modal with ETA and courier details.
   - Add filtering and search by hospital/blood type.

4. Accessibility & styling
   - Improve ARIA attributes, color contrast, and keyboard navigation.

5. Tests
   - Add basic UI tests (Playwright or Cypress) for critical flows (submit emergency, visualize transfer).
